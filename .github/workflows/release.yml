name: Create GitHub Release

# Triggers when a semver tag (v1.2.3, v1.2.3-alpha.1, etc.) is pushed.
# The tag is typically created by semantic-release.yml (automated bump from
# conventional commits) or manually via `git tag && git push --tags`.
# Produces a GitHub Release with auto-generated notes from merged PRs.
# The docker-publish workflow handles the GHCR image; this handles the release page.

on:
  push:
    tags:
      - "v[0-9]*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write   # required to create the release

jobs:
  release:
    name: "📦 GitHub Release"
    runs-on: ubuntu-latest

    steps:
      - name: "job-start"
        run: |
          echo "job-start: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "sha: ${{ github.sha }}"

      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0   # full history so --generate-notes can link PRs

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --title "Release ${{ github.ref_name }}" \
            --generate-notes \
            --verify-tag

      - name: "job-finish"
        if: always()
        run: |
          echo "job-finish: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "sha: ${{ github.sha }}"
